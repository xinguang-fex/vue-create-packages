/*********************************************
 * 文件规范:
 * 方法名称命名规范: 动词开头,尽量首字母大写
 * 作者: Catherine
 ********************************************/
import fetch from '../config/fetch'

import {
    baseUrl
} from '../config/envconf'

// 接口调用样例
// export const login = data => fetch('login', data, "POST");

